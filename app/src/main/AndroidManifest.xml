<?xml version="1.0" encoding="utf-8"?>
<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.example.project_ad"
	>

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/Theme.Project_AD"
		>
		<activity android:name=".MainActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity android:name=".ChoiceActivity" />
		<activity android:name=".SpellingChoiceActivity"/>
		<activity android:name=".EasySpellingActivity"/>
		<activity android:name=".ToFChoiceActivity"/>
		<activity android:name=".EasyTofActivity"/>
		<activity android:name=".FindCoupActivity"/>
		<activity android:name=".ScoresActivity"/>
	</application>

</manifest>